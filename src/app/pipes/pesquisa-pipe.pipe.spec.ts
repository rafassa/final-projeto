import { PesquisaPipePipe } from './pesquisa-pipe.pipe';

describe('PesquisaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PesquisaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
